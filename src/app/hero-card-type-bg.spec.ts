import { HeroCardTypeBgPipe } from './hero-card-type-bg.pipe';

describe('HeroCardTypeBgPipe', () => {
  it('create an instance', () => {
    const pipe = new HeroCardTypeBgPipe();
    expect(pipe).toBeTruthy();
  });
});