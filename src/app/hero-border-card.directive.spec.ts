import { HeroBorderCardDirective } from './hero-border-card.directive';

describe('HeroBorderCardDirective', () => {
  it('should create an instance', () => {
    const directive = new HeroBorderCardDirective();
    expect(directive).toBeTruthy();
  });
});
