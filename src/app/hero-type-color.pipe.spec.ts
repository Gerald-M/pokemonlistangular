import { HeroTypeColorPipe } from './hero-type-color.pipe';

describe('HeroTypeColorPipe', () => {
  it('create an instance', () => {
    const pipe = new HeroTypeColorPipe();
    expect(pipe).toBeTruthy();
  });
});
