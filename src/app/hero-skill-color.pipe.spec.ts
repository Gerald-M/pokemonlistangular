import { HeroSkillColorPipe } from './hero-skill-color.pipe';

describe('HeroSkillColorPipe', () => {
  it('create an instance', () => {
    const pipe = new HeroSkillColorPipe();
    expect(pipe).toBeTruthy();
  });
});
